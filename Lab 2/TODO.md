# Lab 2 TODO

- [x] Inspect existing folder structure under `Lab 2`
- [x] Create `Vehicle.java` in package `cit.cos202.lab2`
- [x] Create `VehicleLab.java` driver in package `cit.cos202.lab2`
- [x] Add `resetCounter()` and include production-danger comment
- [ ] Verify compilation (run `javac`) and fix any package/path issues
- [ ] Ensure `toString()` matches required format exactly
- [ ] Ensure validation rules are implemented correctly

